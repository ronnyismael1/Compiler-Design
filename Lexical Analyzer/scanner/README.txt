To Build:
    run g++ -o scanner.cpp main.cpp
    OR
    Run the given Makefile with your editor of choice.

To Run:
    cd into the assignment's directory after building and run: 
        (for windows)./scanner.exe 
        (for linux) ./scanner.exe (If compiled as windows exe, install wine: sudo apt install wine - Then run wine ./scanner.exe)